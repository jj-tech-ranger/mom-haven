import React,{useEffect,useMemo,useState} from 'react';
import {ChevronLeft,Check,Circle,Baby,Heart,Syringe,Scale,Smile,Loader2} from 'lucide-react';
import {collection,onSnapshot} from 'firebase/firestore';
import {db} from '../../lib/firebase';
import {ChildDoc,DevelopmentRecordDoc,GrowthMeasurementDoc,ImmunizationRecordDoc,NewbornRecordDoc,PostnatalEncounterDoc} from '../../types';
import {formatKenyaDate,formatChildAge,ageInDays} from '../../utils/dateFormats';
import EmptyState from '../EmptyState';
interface ChildTimelineProps{child?:ChildDoc|null;onBack:()=>void;onSelectCategory?: (category:string)=>void}
type Category='newborn'|'pnc'|'immunization'|'growth'|'development';type TimelineItem={id:string;date:Date;ageLabel:string;category:Category;title:string;detail:string};
const iconFor=(category:Category)=>category==='newborn'?Baby:category==='pnc'?Heart:category==='immunization'?Syringe:category==='growth'?Scale:Smile;
export const ChildTimeline:React.FC<ChildTimelineProps>=({child,onBack,onSelectCategory})=>{const[filter,setFilter]=useState<'all'|Category>('all');const[newbornRecords,setNewbornRecords]=useState<NewbornRecordDoc[]>([]);const[pncRecords,setPncRecords]=useState<PostnatalEncounterDoc[]>([]);const[immunizations,setImmunizations]=useState<ImmunizationRecordDoc[]>([]);const[growthRecords,setGrowthRecords]=useState<GrowthMeasurementDoc[]>([]);const[developmentRecords,setDevelopmentRecords]=useState<DevelopmentRecordDoc[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{if(!child?.id){setLoading(false);return}setLoading(true);let loaded=0;const done=()=>{loaded+=1;if(loaded===5)setLoading(false)};const base=['newbornRecords','postnatalEncounters','immunizationRecords','growthMeasurements','developmentRecords'] as const;const unsubs=base.map(subcollection=>onSnapshot(collection(db,'children',child.id,subcollection),snapshot=>{if(subcollection==='newbornRecords')setNewbornRecords(snapshot.docs.map(d=>({id:d.id,...(d.data() as Omit<NewbornRecordDoc,'id'>)})));if(subcollection==='postnatalEncounters')setPncRecords(snapshot.docs.map(d=>({id:d.id,...(d.data() as Omit<PostnatalEncounterDoc,'id'>)})));if(subcollection==='immunizationRecords')setImmunizations(snapshot.docs.map(d=>({id:d.id,...(d.data() as Omit<ImmunizationRecordDoc,'id'>)})));if(subcollection==='growthMeasurements')setGrowthRecords(snapshot.docs.map(d=>({id:d.id,...(d.data() as Omit<GrowthMeasurementDoc,'id'>)})));if(subcollection==='developmentRecords')setDevelopmentRecords(snapshot.docs.map(d=>({id:d.id,...(d.data() as Omit<DevelopmentRecordDoc,'id'>)})));done()},()=>done()));return()=>unsubs.forEach(unsubscribe=>unsubscribe())},[child?.id]);const items=useMemo<TimelineItem[]>(()=>{if(!child)return[];const birth=new Date(`${child.dateOfBirth}T00:00:00`);const result:TimelineItem[]=[{id:'newborn-period',date:birth,ageLabel:'Birth–28 days',category:'newborn',title:'Newborn period',detail:'The first 28 days of your child’s journey.'}];newbornRecords.forEach(record=>{const date=new Date(record.date);result.push({id:`newborn-${record.id}`,date,ageLabel:formatChildAge(child.dateOfBirth,date),category:'newborn',title:'Newborn record',detail:`Recorded ${formatKenyaDate(date)}.`})});pncRecords.forEach(record=>{const date=new Date(record.date);result.push({id:`pnc-${record.id}`,date,ageLabel:formatChildAge(child.dateOfBirth,date),category:'pnc',title:`PNC · ${record.visit}`,detail:`Postnatal contact recorded ${formatKenyaDate(date)}.`})});immunizations.forEach(record=>{if(!record.dateGiven)return;const date=new Date(record.dateGiven);result.push({id:`immunization-${record.id}`,date,ageLabel:formatChildAge(child.dateOfBirth,date),category:'immunization',title:`${record.vaccine} · ${record.dose}`,detail:`Dose recorded ${formatKenyaDate(date)}.`})});growthRecords.forEach(record=>{const date=new Date(record.date);result.push({id:`growth-${record.id}`,date,ageLabel:formatChildAge(child.dateOfBirth,date),category:'growth',title:'Growth check-in',detail:`${record.weightKg} kg${record.heightCm?` · ${record.heightCm} cm`:''} recorded ${formatKenyaDate(date)}.`})});developmentRecords.forEach(record=>{const date=new Date(record.date);result.push({id:`development-${record.id}`,date,ageLabel:formatChildAge(child.dateOfBirth,date),category:'development',title:record.milestoneTitle,detail:`${record.achieved?'Achieved':'Not yet achieved'} · recorded ${formatKenyaDate(date)}.`})});return result.sort((a,b)=>a.date.getTime()-b.date.getTime())},[child,newbornRecords,pncRecords,immunizations,growthRecords,developmentRecords]);const visibleItems=filter==='all'?items:items.filter(item=>item.category===filter);const nowId=useMemo(()=>{if(!child||!items.length)return null;if(ageInDays(child.dateOfBirth)<=28)return'newborn-period';const datedRecords=items.filter(item=>item.id!=='newborn-period');return datedRecords.length?datedRecords[datedRecords.length-1].id:null},[child,items]);if(!child)return <div className="min-h-screen bg-lavender-50 p-5"><button onClick={onBack} className="w-10 h-10 rounded-full bg-white border border-border-hairline text-haven-deep flex items-center justify-center"><ChevronLeft className="w-5 h-5"/></button><div className="mt-10"><EmptyState icon={Baby} title="No child selected" message="Add a child record before opening the first-five-years journey."/></div></div>;return <div className="min-h-screen bg-lavender-50 pb-24"><header className="sticky top-0 z-20 bg-white/95 backdrop-blur border-b border-border-hairline px-4 py-3.5 flex items-center gap-3"><button onClick={onBack} className="w-10 h-10 rounded-full bg-lavender-100 border border-border-hairline text-haven-deep flex items-center justify-center" aria-label="Back"><ChevronLeft className="w-5 h-5"/></button><div><h1 className="font-display font-bold text-xl text-ink-900">Child Timeline</h1><p className="font-body text-xs text-ink-600">First five years · real recorded care</p></div></header><main className="p-4 space-y-4 max-w-lg mx-auto"><div className="flex gap-2 overflow-x-auto pb-1">{(['all','newborn','pnc','immunization','growth','development'] as const).map(category=><button key={category} onClick={()=>{setFilter(category);if(category!=='all')onSelectCategory?.(category)}} className={`px-3 py-1.5 rounded-pill whitespace-nowrap text-xs font-display font-bold ${filter===category?'bg-haven-deep text-white':'bg-white text-ink-700 border border-border-hairline'}`}>{category==='all'?'All':category[0].toUpperCase()+category.slice(1)}</button>)}</div>{loading?<div className="bg-white rounded-[20px] border border-border-hairline p-8 flex flex-col items-center gap-2 text-ink-600"><Loader2 className="w-6 h-6 animate-spin text-haven-orchid"/><span className="font-body text-sm">Loading your recorded milestones…</span></div>:visibleItems.length===0?<EmptyState icon={Baby} title="No milestones in this area yet" message="Recorded newborn care, PNC, immunizations, growth and development will appear here as you add them."/>:<section className="bg-white rounded-[20px] border border-border-hairline shadow-card-1 p-5 relative overflow-hidden"><div className="relative z-10 space-y-5">{visibleItems.map(item=>{const isPast=item.id!==nowId&&item.date.getTime()<Date.now();const isNow=item.id===nowId;const Icon=iconFor(item.category);return <button key={item.id} onClick={()=>onSelectCategory?.(item.category)} className="w-full flex items-start gap-4 text-left p-2 rounded-2xl"><div className="w-8 flex-shrink-0 flex justify-center">{isNow?<span className="w-9 h-9 rounded-full border-2 border-haven-orchid bg-white flex items-center justify-center"><span className="w-4 h-4 rounded-full bg-haven-orchid"/></span>:isPast?<span className="w-8 h-8 rounded-full bg-haven-deep text-white flex items-center justify-center"><Check className="w-4 h-4 stroke-[3]"/></span>:<span className="w-8 h-8 rounded-full border-2 border-[#D8CEE8] bg-white flex items-center justify-center"><Circle className="w-2 h-2 text-[#D8CEE8]"/></span>}</div><div className="flex-1"><div className="flex items-center gap-2 flex-wrap"><span className={`font-display font-bold text-sm ${isPast||isNow?'text-ink-900':'text-ink-600'}`}>{item.title}</span>{isNow&&<span className="px-2 py-0.5 rounded-pill bg-haven-orchid text-white text-[10px] font-display font-bold">Now</span>}</div><p className="font-body text-xs text-ink-600 mt-1">{item.detail}</p><span className="inline-flex items-center gap-1 mt-1 text-[10px] text-ink-600"><Icon className="w-3 h-3 text-haven-orchid"/>{item.ageLabel}</span></div></button>})}</div></section>}</main></div>};
