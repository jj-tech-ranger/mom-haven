import React,{useState} from 'react';
import {ArrowLeft,Save,Building2,Users,Car,AlertTriangle} from 'lucide-react';
import {BirthPlanLogistics} from '../../types';
interface Props{initialPlan?:BirthPlanLogistics|null;onBack:()=>void;onSave:(p:BirthPlanLogistics)=>Promise<void>}
const Input=({label,value,onChange,textarea=false,required=false}:{label:string;value:string;onChange:(v:string)=>void;textarea?:boolean;required?:boolean})=><label className="block font-body text-xs text-ink-600">{label}{textarea?<textarea required={required} value={value} onChange={e=>onChange(e.target.value)} rows={3} className="mt-1 w-full p-3 rounded-xl border border-border-hairline bg-lavender-50/50 text-sm text-ink-900"/>:<input required={required} value={value} onChange={e=>onChange(e.target.value)} className="mt-1 w-full p-3 rounded-xl border border-border-hairline bg-lavender-50/50 text-sm text-ink-900"/>}</label>;
export const EditBirthPlan:React.FC<Props>=({initialPlan,onBack,onSave})=>{const [p,setP]=useState<BirthPlanLogistics>(initialPlan||{preferredFacility:'',supportPersonName:'',transportPlan:'',emergencyLogistics:'',notes:''});const [saving,setSaving]=useState(false);const set=(k:keyof BirthPlanLogistics,v:string)=>setP(x=>({...x,[k]:v}));const submit=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);try{await onSave(p)}finally{setSaving(false)}};return <div className="min-h-screen bg-lavender-50 pb-24"><header className="sticky top-0 z-20 bg-white/95 backdrop-blur border-b border-border-hairline px-4 py-3.5 flex items-center gap-3"><button type="button" onClick={onBack} className="w-10 h-10 rounded-full bg-lavender-100 flex items-center justify-center text-haven-deep"><ArrowLeft className="w-5 h-5"/></button><div><h1 className="font-display font-bold text-xl text-ink-900">Edit birth plan</h1><p className="font-body text-xs text-ink-600">Keep the plan practical and up to date</p></div></header><form onSubmit={submit} className="p-4 max-w-[420px] mx-auto space-y-4"><div className="bg-white rounded-[20px] border border-border-hairline p-5 space-y-4"><div className="flex items-center gap-2 font-display font-bold text-sm text-haven-deep"><Building2 className="w-4 h-4"/>Facility</div><Input label="Preferred facility" value={p.preferredFacility} required onChange={v=>set('preferredFacility',v)}/></div><div className="bg-white rounded-[20px] border border-border-hairline p-5 space-y-4"><div className="flex items-center gap-2 font-display font-bold text-sm text-haven-deep"><Users className="w-4 h-4"/>Support</div><Input label="Support person name" value={p.supportPersonName} required onChange={v=>set('supportPersonName',v)}/></div><div className="bg-white rounded-[20px] border border-border-hairline p-5 space-y-4"><div className="flex items-center gap-2 font-display font-bold text-sm text-haven-deep"><Car className="w-4 h-4"/>Transport</div><Input label="Transport plan" value={p.transportPlan} required onChange={v=>set('transportPlan',v)}/><p className="text-[11px] text-ink-600">Transport is logistics only. This field is intentionally structured so a connected Partner can become its editor in a later phase.</p></div><div className="bg-white rounded-[20px] border border-border-hairline p-5 space-y-4"><div className="flex items-center gap-2 font-display font-bold text-sm text-haven-deep"><AlertTriangle className="w-4 h-4"/>Emergency logistics</div><Input label="Emergency logistics" value={p.emergencyLogistics} required onChange={v=>set('emergencyLogistics',v)}/><Input label="Notes" value={p.notes} textarea onChange={v=>set('notes',v)}/></div><button disabled={saving} className="w-full py-3.5 rounded-pill bg-gradient-to-r from-haven-deep to-haven-orchid text-white font-display font-bold flex items-center justify-center gap-2"><Save className="w-4 h-4"/>{saving?'Saving…':'Save birth plan'}</button></form></div>};
