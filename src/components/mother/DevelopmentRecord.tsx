import React,{useState} from 'react';import {ArrowLeft,Save} from 'lucide-react';import {addDoc,collection} from 'firebase/firestore';import {auth,db} from '../../lib/firebase';import {Provenance} from '../../types';
interface Props{childId:string;onBack:()=>void;onSave?:(r:any)=>Promise<void>|void}
export const DevelopmentRecord:React.FC<Props>=({childId,onBack})=>{const [title,setTitle]=useState(''),[age,setAge]=useState(''),[achieved,setAchieved]=useState(true),[notes,setNotes]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!auth.currentUser||!title||!age)return;const provenance:Provenance={status:'REPORTED',enteredBy:auth.currentUser.uid,enteredAt:new Date().toISOString(),verifiedBy:null,verifiedAt:null,source:'reported_caregiver'};await addDoc(collection(db,'children',childId,'developmentRecords'),{childId,date:new Date().toISOString().slice(0,10),milestoneTitle:title,ageCategory:age,achieved,notes,provenance});onBack()};return <div className="min-h-screen bg-lavender-50 pb-24"><header className="sticky top-0 z-20 bg-white/95 backdrop-blur border-b border-border-hairline px-4 py-3.5 flex items-center gap-3"><button onClick={onBack} className="w-10 h-10 rounded-full bg-lavender-100 flex items-center justify-center text-haven-deep"><ArrowLeft className="w-5 h-5"/></button><h1 className="font-display font-bold text-xl text-ink-900">Development record</h1></header><form onSubmit={submit} className="p-4 max-w-[420px] mx-auto space-y-4"><div className="bg-white rounded-[20px] border border-border-hairline p-5 space-y-4"><label className="font-body text-xs text-ink-600">Milestone<input required value={title} onChange={e=>setTitle(e.target.value)} placeholder="What did you observe?" className="mt-1 w-full p-3 rounded-xl border border-border-hairline"/></label><label className="font-body text-xs text-ink-600">Age band<input required value={age} onChange={e=>setAge(e.target.value)} placeholder="e.g. 4–6 months" className="mt-1 w-full p-3 rounded-xl border border-border-hairline"/></label><label className="flex items-center gap-3 font-body text-sm text-ink-900"><input type="checkbox" checked={achieved} onChange={e=>setAchieved(e.target.checked)}/>Observed / achieved</label><label className="font-body text-xs text-ink-600">Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={4} className="mt-1 w-full p-3 rounded-xl border border-border-hairline"/></label></div><button className="w-full py-3.5 rounded-pill bg-gradient-to-r from-haven-deep to-haven-orchid text-white font-display font-bold flex items-center justify-center gap-2"><Save className="w-4 h-4"/>Save record</button></form></div>};
