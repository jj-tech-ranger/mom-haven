import React, { useState } from 'react';
import { X, Syringe, Info, AlertCircle } from 'lucide-react';
import { ChildVaccineRecord, Provenance } from '../../types';
import { KENYA_FACILITIES } from '../../data/kenyaFacilities';
import { KEPI_SCHEDULE } from './ImmunizationPassport';
import { collection, addDoc } from 'firebase/firestore';
import { db } from '../../lib/firebase';
import Button from '../Button';

interface AddVaccineModalProps { childId: string; userId: string; initialVaccineName?: string; initialAgeBracket?: string; onClose: () => void; onSaved: () => void; }

export default function AddVaccineModal({ childId, userId, initialVaccineName = '', initialAgeBracket = '', onClose, onSaved }: AddVaccineModalProps) {
  const [vaccineName, setVaccineName] = useState(initialVaccineName); const [recommendedAgeBracket, setRecommendedAgeBracket] = useState(initialAgeBracket);
  const [dateAdministered, setDateAdministered] = useState(''); const [facilityName, setFacilityName] = useState(''); const [batchNumber, setBatchNumber] = useState('');
  const [administeredBy, setAdministeredBy] = useState(''); const [adverseEvents, setAdverseEvents] = useState(''); const [notes, setNotes] = useState('');
  const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null);
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); try { setLoading(true); setError(null); const provenance: Provenance = { status: 'REPORTED', enteredBy: userId, enteredAt: new Date().toISOString(), verifiedBy: null, verifiedAt: null }; await addDoc(collection(db, 'childVaccineRecords'), { childId, vaccineName, recommendedAgeBracket, dateAdministered, facilityName, batchNumber: batchNumber.trim() || undefined, administeredBy: administeredBy.trim() || undefined, status: 'GIVEN', adverseEvents: adverseEvents.trim(), notes: notes.trim(), provenance, createdAt: new Date().toISOString() }); onSaved(); onClose(); } catch (err: any) { console.error('Failed to record vaccine', err); setError(err?.message || 'Failed to save vaccine record.'); } finally { setLoading(false); } };
  return <div className="fixed inset-0 z-50 bg-black/50 backdrop-blur-xs flex items-end sm:items-center justify-center p-0 sm:p-4"><div className="bg-white rounded-t-[28px] sm:rounded-[24px] w-full max-w-md p-6 shadow-2xl max-h-[90vh] overflow-y-auto"><div className="flex items-center justify-between pb-3 border-b border-[var(--border-hairline)]"><div className="flex items-center gap-2"><div className="w-8 h-8 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-800"><Syringe className="w-4 h-4" /></div><h2 className="font-display font-extrabold text-[18px] text-[var(--ink-900)]">Log Administered Vaccine</h2></div><button type="button" onClick={onClose} className="w-8 h-8 rounded-full bg-[var(--lavender-50)] flex items-center justify-center cursor-pointer"><X className="w-4 h-4" /></button></div><div className="mt-3 p-3.5 rounded-[16px] bg-[#FBF0DC] border border-[#A15E06]/30 flex items-start gap-2.5 text-[12px] text-[#A15E06]"><Info className="w-4 h-4 shrink-0 mt-0.5" /><div><span className="font-bold font-display block">Self-Reported Entry</span>This dose remains self-reported until confirmed by a healthcare provider.</div></div>{error && <div className="mt-3 p-3 bg-red-50 border border-red-200 text-red-700 text-[13px] rounded-[14px] flex items-start gap-2"><AlertCircle className="w-4 h-4" /><span>{error}</span></div>}<form onSubmit={handleSubmit} className="space-y-4 pt-3"><select value={vaccineName} onChange={e => { setVaccineName(e.target.value); const match = KEPI_SCHEDULE.find(s => s.name === e.target.value); if (match) setRecommendedAgeBracket(match.ageBracket); }} required className="w-full px-3 py-2 rounded-[14px] border border-[var(--border-hairline)] bg-white text-[13px]"><option value="">Select vaccine</option>{KEPI_SCHEDULE.map(s => <option key={s.id} value={s.name}>{s.name} ({s.ageBracket})</option>)}</select><input type="date" value={dateAdministered} onChange={e => setDateAdministered(e.target.value)} required className="w-full px-3 py-2 rounded-[14px] border border-[var(--border-hairline)] bg-white text-[13px]" /><input type="text" value={facilityName} onChange={e => setFacilityName(e.target.value)} placeholder="Health facility" required className="w-full px-3 py-2 rounded-[14px] border border-[var(--border-hairline)] bg-white text-[13px]" /><input type="text" value={batchNumber} onChange={e => setBatchNumber(e.target.value)} placeholder="Batch / lot number (optional)" className="w-full px-3 py-2 rounded-[14px] border border-[var(--border-hairline)] bg-white text-[13px]" /><input type="text" value={administeredBy} onChange={e => setAdministeredBy(e.target.value)} placeholder="Provider name (optional)" className="w-full px-3 py-2 rounded-[14px] border border-[var(--border-hairline)] bg-white text-[13px]" /><input type="text" value={adverseEvents} onChange={e => setAdverseEvents(e.target.value)} placeholder="Side effects / notes" className="w-full px-3 py-2 rounded-[14px] border border-[var(--border-hairline)] bg-white text-[13px]" /><Button type="submit" variant="primary" disabled={loading} className="w-full py-3.5">{loading ? 'Saving...' : 'Record Vaccine Dose'}</Button></form></div></div>;
}
